{
  "name": "brick-by-brick-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:pages": "cross-env BASE_PATH=/brick-by-brick-site/ vite build",
    "deploy:pages": "npm run build:pages && gh-pages -d dist --nojekyll"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "cross-env": "^7.0.3",
    "gh-pages": "^6.0.0",
    "vite": "^7.0.0"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
